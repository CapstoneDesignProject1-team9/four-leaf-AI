# ruff.toml — Python 린터/포매터 설정
target-version = "py312"
line-length = 100

[lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = [
    "E501",  # line too long (formatter가 처리)
]

[format]
quote-style = "double"
indent-style = "space"
